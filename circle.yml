machine:
  timezone:
    America/Los_Angeles
  node:
    version: 5.1.0
  environment:
    NODE_ENV: test
    PORT: 3001
dependencies:
  pre:
    - npm install -g node-gyp@latest
    - npm start
test:
  override:
    - mocha ./test
deployment:
  production:
    branch: master
    heroku:
      appname: cmp-api
